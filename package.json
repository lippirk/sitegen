{
  "name": "content",
  "version": "0.0.0",
  "description": "",
  "scripts": {
    "start": "parcel index.pug",
    "start-unspec": "parcel index.pug --dist-dir=dist --host 0.0.0.0",
    "build": "parcel build index.pug --dist-dir=build --no-source-maps --no-cache",
    "test": "parcel build --help"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-inline-string": "^2.6.0",
    "@parcel/transformer-pug": "^2.6.0",
    "parcel": "^2.6.0",
    "parcel-namer-namer": "file:parcel-namer-namer",
    "parcel-transformer-pandoc": "file:parcel-transformer-pandoc"
  },
  "dependencies": {
    "@svgdotjs/svg.js": "^3.1.1"
  }
}
